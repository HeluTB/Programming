class Solution:
    def appendCharacters(self, s: str, t: str) -> int:
        i = 0
        n = len(t)
        for char in s:
            if i < n and char == t[i]:
                i += 1
            
        return n - i