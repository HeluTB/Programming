class Solution:
    def reductionOperations(self, nums: List[int]) -> int:
        n = len(nums)
        count, red = 0, 0
        nums.sort()
        
        for i in range(1, n):
            if nums[i] > nums[i - 1]:
                red += 1
            count += red
        
        return count