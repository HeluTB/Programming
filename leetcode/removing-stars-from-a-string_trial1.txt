class Solution:
    def removeStars(self, s: str) -> str:
        my_stack = []
        for char in s:
            if char == '*':
                if my_stack:
                    my_stack.pop()
            else:
                my_stack.append(char)

        return "".join(my_stack)


